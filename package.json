{
    "name": "toml-edit-js",
    "type": "module",
    "version": "0.1.1",
    "private": true,
    "packageManager": "pnpm@9.6.0",
    "scripts": {
        "build": "wasmup build .",
        "bump": "bumpp --all -x 'pnpm pre-publish'",
        "pre-publish": "pnpm bump:cargo && pnpm changelog",
        "bump:cargo": "tsx scripts/bump_cargo.ts",
        "changelog": "conventional-changelog -si CHANGELOG.md",
        "lint": "eslint .",
        "prepare": "husky",
        "release": "pnpm bump && pnpm build && pnpm -r publish",
        "test": "vitest",
        "test:wasm": "wasm-pack test --headless --chrome",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "@commitlint/config-conventional": "^19.2.2",
        "@rainbowatcher/eslint-config": "^0.12.0",
        "@rainbowatcher/eslint-config-ignore": "^0.12.0",
        "@rainbowatcher/eslint-config-js": "^0.12.0",
        "@rainbowatcher/eslint-config-json": "^0.12.0",
        "@rainbowatcher/eslint-config-md": "^0.12.0",
        "@rainbowatcher/eslint-config-prettier": "^0.12.0",
        "@rainbowatcher/eslint-config-toml": "^0.12.0",
        "@rainbowatcher/eslint-config-ts": "^0.12.0",
        "@types/node": "^22.0.2",
        "bumpp": "^9.4.1",
        "commitlint": "^19.3.0",
        "conventional-changelog-cli": "^5.0.0",
        "dedent": "^1.5.3",
        "eslint": "^9.8.0",
        "husky": "^9.1.4",
        "lint-staged": "^15.2.7",
        "picocolors": "^1.0.1",
        "tsx": "^4.16.4",
        "typescript": "^5.5.4",
        "vitest": "^2.0.5",
        "wasmup": "^0.5.0"
    },
    "lint-staged": {
        "*.{js,ts,json,toml,md}": "eslint"
    }
}
